# 🌸 ReimuSync: Universal Minecraft Dotfiles Installer
Простенькая утилита на базе **PowerShell WinForms**, которая делает бэкап существующей папки `.minecraft` и копирует туда ту, которая в директории рядом с приложением.

## Запуск
-   Положи папку `.minecraft` в ту же директорию, где лежит `ReimuSync.exe`.

-   Запусти `ReimuSync.exe`.

## Сборка

 1. Установи [Win-PS2EXE](https://github.com/MScholtes/Win-PS2EXE).
 `Install-Module ps2exe`
 
 2. Скомпилируй скрипт в той же директории.
 `ps2exe .\sync_minecraft.ps1 .\ReimuSync.exe -noConsole -x64`

### ⚠️ Важное примечание

Убедись, что файл `.ps1` сохранен в кодировке **UTF-8 с BOM** или **UTF-8 со спецификацией**, иначе дот-арт с Рейму превратится в тыкву (квадратики).